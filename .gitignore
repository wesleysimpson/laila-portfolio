node_modules/
.DS_Store
*.log
_site/

# Original camera-named uploads left in the project root.
# Clean, renamed copies already live in images/raw/ — these root duplicates are
# kept on disk as a personal backup but are not committed or deployed.
# Safe to delete whenever you like.
/*.jpeg
/*.JPG

# …but DO track these root-located source images — the build (EXTRA_SOURCES in
# scripts/optimize-images.js) optimizes them into images/optimized/, so they must
# be committed for the GitHub Pages deploy to find them.
!/studio1.JPG
!/studio2.JPG
!/studio3.JPG
!/test_bts.JPG

images/archive/
